
/**
 * Default number of records to be sent in list view response
 */
const records_per_page = 10;

/**
 * Default starting index value
 */
const starting_index = 0;

module.exports = {
    records_per_page,
    starting_index
}